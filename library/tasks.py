"""Celery tasks for the library app.

Placeholder module for background work (e.g. overdue-loan reminder emails,
nightly sweep flagging ``borrowed`` loans past ``due_on`` as ``overdue``). No
async jobs are required for the current endpoints; tasks are added here as the
module grows.
"""
